<fieldset>
  <legend><%= comment.user.try(:username).capitalize %></legend>
    <ul>
      <li><%= comment.comment %></li>
      <li><h6><%= link_to 'Delete', comment_path(comment, commentable_type: commentable.class.to_s.underscore, commentable_id: commentable.id), method: :delete, data: { confirm: 'Are you sure?' } %></h6></li>
    </ul>
</fieldset>